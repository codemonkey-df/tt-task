# RAG Extension (Deferred)

Phase 2: unstructured invoice PDF ingest via LLM extraction.
Not required — challenge data is structured JSON.

See architecture discussion: LLM at ingest for raw documents,
deterministic rules for detection, RAG for contract policy retrieval.
